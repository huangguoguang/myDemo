<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd " >
<mapper	namespace="com.tarena.dao.IPaymentTailDao">
	<select id="getGoodDetail" parameterType="map" resultType="map">
		SELECT * FROM t_wp_front_shopping_order WHERE shopping_order_id = #{shopping_order_id} and user_id=#{user_id}
	</select>
</mapper>
